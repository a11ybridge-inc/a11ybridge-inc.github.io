/* ============================================================
   A11YBRIDGE — Component Styles
   ============================================================ */

/* ── Skip Link ─────────────────────────────────────────── */
.skip-link {
  position: absolute;
  top: var(--space-4);
  left: var(--space-4);
  z-index: 9999;
  padding: var(--space-3) var(--space-6);
  background: var(--color-primary);
  color: #fff;
  font-weight: 700;
  font-size: var(--text-sm);
  border-radius: var(--radius-md);
  transform: translateY(-200%);
  transition: transform var(--transition);
  text-decoration: none;
}
.skip-link:focus { transform: translateY(0); }

/* ── Header ────────────────────────────────────────────── */
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: var(--color-bg);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--color-divider);
  transition: box-shadow 0.25s var(--ease-out);
}
.site-header.scrolled { box-shadow: var(--shadow-sm); }

.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-8);
  padding-block: var(--space-4);
}

/* Logo */
.site-logo {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  text-decoration: none;
  color: var(--color-text);
  flex-shrink: 0;
}
.site-logo:hover { color: var(--color-primary); }
.logo-mark { flex-shrink: 0; }
.logo-wordmark {
  font-family: var(--font-display);
  font-size: 1.25rem;
  font-weight: 800;
  letter-spacing: -0.02em;
  color: var(--color-text);
  line-height: 1;
}
.logo-wordmark span { color: var(--color-primary); }

/* Nav */
.site-nav { display: flex; align-items: center; gap: var(--space-2); }

.nav-links {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  list-style: none;
}

.nav-links a {
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--color-text-muted);
  text-decoration: none;
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-md);
  transition: color var(--transition), background var(--transition);
}
.nav-links a:hover,
.nav-links a[aria-current="page"] {
  color: var(--color-primary);
  background: var(--color-primary-light);
}

/* Dark mode nav: add underline so links are distinguishable from
   surrounding text by more than colour alone (WCAG 1.4.1 + axe link-in-text-block) */
[data-theme="dark"] .nav-links a {
  color: #8fbfbf;
  text-decoration: underline;
  text-underline-offset: 3px;
  text-decoration-color: rgba(143, 191, 191, 0.5);
}
[data-theme="dark"] .nav-links a:hover,
[data-theme="dark"] .nav-links a[aria-current="page"] {
  color: var(--color-primary);
  text-decoration-color: var(--color-primary);
}

@media (prefers-color-scheme: dark) {
  :root:not([data-theme]) .nav-links a {
    color: #8fbfbf;
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: rgba(143, 191, 191, 0.5);
  }
  :root:not([data-theme]) .nav-links a:hover,
  :root:not([data-theme]) .nav-links a[aria-current="page"] {
    color: #4ec9d1;
    text-decoration-color: #4ec9d1;
  }
}

.nav-cta {
  margin-left: var(--space-4);
  font-size: var(--text-sm);
  padding: 0.625rem 1.25rem;
  min-height: 40px;
}

/* Dark mode toggle */
.theme-toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: var(--radius-md);
  color: var(--color-text-muted);
  background: transparent;
  border: 1px solid var(--color-border);
  transition: color var(--transition), background var(--transition), border-color var(--transition);
  flex-shrink: 0;
}
.theme-toggle:hover {
  color: var(--color-primary);
  background: var(--color-primary-light);
  border-color: var(--color-primary-mid);
}

/* Mobile menu button */
.menu-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: var(--radius-md);
  color: var(--color-text);
  background: transparent;
  border: 1px solid var(--color-border);
}

@media (max-width: 768px) {
  .menu-toggle { display: flex; }
  .nav-links { display: none; }
  .nav-cta { display: none; }

  .nav-links.open {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    position: fixed;
    top: 73px;
    left: 0;
    right: 0;
    bottom: 0;
    background: var(--color-bg);
    padding: var(--space-8) var(--space-6);
    gap: var(--space-2);
    z-index: 99;
    border-top: 1px solid var(--color-divider);
  }
  .nav-links.open a {
    font-size: var(--text-base);
    padding: var(--space-4) var(--space-3);
    width: 100%;
  }
  .nav-links.open + .nav-cta {
    display: flex !important;
    position: fixed;
    bottom: var(--space-8);
    left: var(--space-6);
    right: var(--space-6);
    z-index: 100;
    justify-content: center;
  }
}

/* ── Footer ────────────────────────────────────────────── */
.site-footer {
  background: var(--color-dark);
  color: #a8c8c9;
  padding-block: var(--space-16) var(--space-10);
}

.footer-grid {
  display: grid;
  grid-template-columns: 1.5fr repeat(3, 1fr);
  gap: var(--space-12);
  margin-bottom: var(--space-12);
}

.footer-brand .logo-wordmark { color: #ffffff; }
.footer-brand .logo-wordmark span { color: #5dcbd2; }
.footer-tagline {
  margin-top: var(--space-4);
  font-size: var(--text-sm);
  color: #7fa8a8;
  max-width: 32ch;
  line-height: 1.6;
}

.footer-col h3 {
  font-size: var(--text-sm);
  font-weight: 700;
  color: #ffffff;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  margin-bottom: var(--space-4);
}

.footer-links {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}
.footer-links a {
  font-size: var(--text-sm);
  color: #7fa8a8;
  text-decoration: none;
  transition: color var(--transition);
}
.footer-links a:hover { color: #5dcbd2; }

.footer-bottom {
  border-top: 1px solid #1e4040;
  padding-top: var(--space-6);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
  flex-wrap: wrap;
}
.footer-copy {
  font-size: var(--text-xs);
  color: #8fbfbf; /* was #4a7070 — 2.74:1 fail → now 4.8:1 on #0d2b2e */
}
.footer-legal {
  display: flex;
  gap: var(--space-6);
}
.footer-legal a {
  font-size: var(--text-xs);
  color: #8fbfbf; /* was #4a7070 — 2.74:1 fail → now 4.8:1 on #0d2b2e */
  text-decoration: underline;
  text-underline-offset: 2px;
}
.footer-legal a:hover { color: #5dcbd2; }

@media (max-width: 768px) {
  .footer-grid {
    grid-template-columns: 1fr 1fr;
    gap: var(--space-8);
  }
  .footer-brand { grid-column: 1 / -1; }
}
@media (max-width: 480px) {
  .footer-grid { grid-template-columns: 1fr; }
}

/* ── Page Hero (inner pages) ───────────────────────────── */
.page-hero {
  background: var(--color-surface-teal);
  padding-block: clamp(var(--space-12), 8vw, var(--space-24));
}
.page-hero-breadcrumb {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  margin-bottom: var(--space-4);
}
.page-hero-breadcrumb a {
  color: var(--color-text-muted);
  text-decoration: none;
}
.page-hero-breadcrumb a:hover { color: var(--color-primary); }
.page-hero h1 {
  font-size: var(--text-2xl);
  font-weight: 800;
  letter-spacing: -0.03em;
  max-width: 18ch;
  margin-bottom: var(--space-4);
}
.page-hero p {
  font-size: var(--text-lg);
  color: var(--color-text-muted);
  max-width: 56ch;
}

/* ── Service Row ───────────────────────────────────────── */
.service-row {
  padding-block: clamp(var(--space-12), 7vw, var(--space-20));
  border-bottom: 1px solid var(--color-divider);
}
.service-row:last-of-type { border-bottom: none; }

.service-row-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: clamp(var(--space-10), 6vw, var(--space-20));
  align-items: center;
}
.service-row-inner.reversed { direction: rtl; }
.service-row-inner.reversed > * { direction: ltr; }

.service-visual {
  border-radius: var(--radius-xl);
  overflow: hidden;
  aspect-ratio: 4/3;
  background: var(--color-surface-teal);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.service-icon-wrap {
  width: 96px;
  height: 96px;
  border-radius: 50%;
  background: var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  position: absolute;
  bottom: var(--space-6);
  right: var(--space-6);
}

.service-content { padding-block: var(--space-4); }
.service-tag {
  display: inline-block;
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-primary);
  margin-bottom: var(--space-4);
}
.service-content h2 {
  font-size: var(--text-xl);
  font-weight: 800;
  letter-spacing: -0.025em;
  margin-bottom: var(--space-4);
}
.service-content .lead {
  font-size: var(--text-lg);
  color: var(--color-text-muted);
  font-weight: 500;
  margin-bottom: var(--space-4);
  max-width: 46ch;
}
.service-content p {
  font-size: var(--text-base);
  color: var(--color-text-muted);
  margin-bottom: var(--space-6);
  max-width: 52ch;
}
.service-link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  font-weight: 700;
  font-size: var(--text-sm);
  color: var(--color-primary);
  text-decoration: none;
  border-bottom: 2px solid var(--color-primary-mid);
  padding-bottom: 2px;
  transition: border-color var(--transition), gap var(--transition);
}
.service-link:hover {
  color: var(--color-primary-hover);
  border-color: var(--color-primary);
  gap: var(--space-3);
}

@media (max-width: 768px) {
  .service-row-inner,
  .service-row-inner.reversed {
    grid-template-columns: 1fr;
    direction: ltr;
  }
}

/* ── Differentiators strip ─────────────────────────────── */
.differentiators {
  background: var(--color-dark);
  color: #fff;
  padding-block: clamp(var(--space-12), 6vw, var(--space-20));
}
.differentiators-header {
  margin-bottom: var(--space-12);
}
.differentiators-header h2 {
  font-size: var(--text-xl);
  font-weight: 800;
  color: #ffffff;
  letter-spacing: -0.03em;
}
.differentiators-header p {
  font-size: var(--text-base);
  color: #7fa8a8;
  margin-top: var(--space-3);
  max-width: 52ch;
}

.diff-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-6);
}

.diff-item {
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: var(--radius-lg);
  padding: var(--space-8);
  transition: background var(--transition), border-color var(--transition);
}
.diff-item:hover {
  background: rgba(61, 184, 192, 0.08);
  border-color: rgba(61,184,192,0.25);
}
.diff-icon {
  width: 48px;
  height: 48px;
  border-radius: var(--radius-md);
  background: rgba(61,184,192,0.15);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #5dcbd2;
  margin-bottom: var(--space-5);
}
.diff-item h3 {
  font-size: var(--text-base);
  font-weight: 700;
  color: #ffffff;
  margin-bottom: var(--space-2);
}
.diff-item p {
  font-size: var(--text-sm);
  color: #7fa8a8;
  line-height: 1.6;
}

@media (max-width: 900px) {
  .diff-grid { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 500px) {
  .diff-grid { grid-template-columns: 1fr; }
}

/* ── CTA Section ───────────────────────────────────────── */
.cta-section {
  background: var(--color-surface-teal);
  padding-block: clamp(var(--space-16), 8vw, var(--space-24));
  text-align: center;
}
.cta-section h2 {
  font-size: var(--text-2xl);
  font-weight: 800;
  letter-spacing: -0.03em;
  margin-bottom: var(--space-5);
  max-width: 22ch;
  margin-inline: auto;
}
.cta-section p {
  font-size: var(--text-base);
  color: var(--color-text-muted);
  max-width: 52ch;
  margin-inline: auto;
  margin-bottom: var(--space-8);
}
.cta-actions {
  display: flex;
  gap: var(--space-4);
  justify-content: center;
  flex-wrap: wrap;
}
